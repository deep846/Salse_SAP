_schema-version: "3.2"
ID: salseorder
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: salseorder-destination-content
  type: com.sap.application.content
  requires:
  - name: salseorder-destination-service
    parameters:
      content-target: true
  - name: salseorder-repo-host
    parameters:
      service-key:
        name: salseorder-repo-host-key
  - name: salseorder-uaa
    parameters:
      service-key:
        name: salseorder-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: salseorder_html_repo_host
          ServiceInstanceName: salseorder-html5-srv
          ServiceKeyName: salseorder-repo-host-key
          sap.cloud.service: salseorder
        - Authentication: OAuth2UserTokenExchange
          Name: salseorder_uaa
          ServiceInstanceName: salseorder-xsuaa-srv
          ServiceKeyName: salseorder-uaa-key
          sap.cloud.service: salseorder
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: salseorder-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: salseorder-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - salseorder.zip
      name: salseorder
      target-path: resources/
- name: salseorder
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: salseorder-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: salseorder-destination-service
    service-plan: lite
- name: salseorder-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: salseorder-xsuaa-srv
    service-plan: application
- name: salseorder-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: salseorder-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
